This is the demo part.
According to the readme file in the model folder, download the model file and store it in the model directory. 
Open the terminal to run the app.py file in python3
Then open the browser and input the url, which is the output on terminal.

tips: Since the model needs time to load, it may spend more time to identify the picture when the first picture is uploaded.
